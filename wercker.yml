box: centos:latest

build:
  steps:
    - script:
      name: print distro info
      code: cat /etc/*release

deploy:
  steps:
    - deogracia/scp-deploy@0.1.4-dev8:
      host: ec2-35-176-228-218.eu-west-2.compute.amazonaws.com
      sshprivatekey: $PK_FILE
      user: ec2-user
      source: ./index.html
      destination: "/var/www"
      hide-ssh-cnx-info-from-log: false
